sudo: required

services: docker

before_install:
  - docker-compose up --build -d 

script:
  - docker-compose exec --env 'RAILS_ENV=test' rails web db:create
  - docker-compose exec --env 'RAILS_ENV=test' rails web db:migrate
  - docker-compose exec --env 'RAILS_ENV=test' rails web test